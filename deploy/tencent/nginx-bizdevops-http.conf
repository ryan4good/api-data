# Install this file in an Nginx include that is loaded directly by the `http`
# context (for example /etc/nginx/conf.d/bizdevops-rate-limit.conf). It must be
# loaded before the server containing nginx-bizdevops.conf.
limit_req_zone $binary_remote_addr zone=bizdevops_login:10m rate=5r/m;
